#include<stdio.h>

int main()
{
	float pie = 3.14f;
	
	float fArea;
	float fRadius;
	
	puts("Enter the radius");
	scanf_s("%f", &fRadius);
	fArea = pie*fRadius*fRadius;
	printf("The Area is:%2f\n", fArea);
	
	getch();
	return 0;
	
}
