#include<stdio.h>

int main()
{
	int num1, num2;
	puts("比较俩个数:");
	scanf_s("%d%d", &num1, &num2);
	if (num1 > num2)
	{
		printf("num1比较大:%d\n", num1);
		
	}
	else
	{
		printf("num2比较大:%d\n", num2);
	}
	
	getch();
	return 0;
}
