#include<stdio.h>

int main()
{
	int iCount, iSum;
	for (iSum = 0, iCount = 0; iCount <= 100; iCount++,iCount++)
	{
		iSum = iSum + iCount;
	}
	printf("the result is:%d\n", iSum);
	getch();
	return 0;
}
