#include<stdio.h>

void DrinkMilk(char* cBottle);
int main()
{
	char cPoke[] = "";
	printf("Mother wanna give the baby.");
	scanf_s("%c", &cPoke);
	DrinkMilk(cPoke);
	getch();
	return 0;
}
void DrinkMilk(char* cBottle)
{
	printf("The Baby drink the %c\n", cBottle);
}
